const runtime = () => {
  // console.log(x);

  // let num = 98;
  // num();



//   JSON Syntax Rules
// JSON requires:

// Property names must be in double quotes (not unquoted)
// String values must be in double quotes (not single quotes)
// let jsonString = '{"name": "John"}';  // ✅ Valid JSON
  // let jsonString = "{name : 'John'}";
  // JSON.parse(jsonString)

//   const fs = require('fs');
//   fs.readFileSync('nonexistentfile.txt');
}

module.exports = runtime